import styled from 'styled-components';

export const ButtonDropDown = styled.button`
  border: 0;
  text-decoration: none;
  outline: none;
  background: none;

  &:focus {
    border: none;
    outline: none;
  }
`;
